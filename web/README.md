# Waaz Realty Web App — Scaffold

Scaffolded Next.js + TypeScript project with Tailwind CSS and support for local TTF fonts.

Quick start:

```bash
# install
npm install

# run dev server
npm run dev
```

Add your .ttf font files to `public/fonts/` (see `public/fonts/README.md`) and restart the dev server.
